# Custom Heroic releases

The feature-manifest build has been replaced by a linear downstream patch series.
See [Downstream builds](downstream.md) for automatic upstream-release tracking,
main-branch builds, signed pacman updates, independent draft PRs, and recovery.
